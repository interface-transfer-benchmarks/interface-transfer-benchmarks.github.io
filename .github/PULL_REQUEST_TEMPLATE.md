<!--
Three kinds of pull request live here: results, a new benchmark, a correction.
Keep the section that applies and delete the other two.
-->

## Results

**Solver**: <!-- the directory under results/, and the method in one line -->

**Benchmarks**: <!-- B-004, B-007, ... -->

| | |
|---|---|
| Ran on | <!-- date --> |
| Grid | <!-- uniform / octree / adaptive, and the resolutions --> |
| Ranks | <!-- serial, or the MPI count --> |
| Swept | <!-- the parameter and its range --> |

- [ ] `julia --project=docs scripts/validate_results.jl` passes
- [ ] `python3 scripts/plot_results.py` draws the figures and I have looked at them
- [ ] The CSV carries `n_per_layer`, or I say below why the case has no layer
- [ ] `Sh` and `Sh_exact` are measurements, not errors
- [ ] Runs that missed their gate are in the data and named in the commentary
- [ ] No file under `figures/` is in this pull request
- [ ] Any new `references.bib` key is in this pull request

**What the numbers say**: <!-- the order you observe, where it degrades, what
you cannot explain. Say it here as well as in <ID>.md if it is worth a
reviewer's attention. -->

---

## New benchmark

**Proposal issue**: <!-- #NN -->

**Title**:

- [ ] Copied from `benchmark-template.md`
- [ ] Every facet declared from the vocabularies in `taxonomy.md`
- [ ] Symbols follow the notation table
- [ ] Reference solution stated, with its provenance and its range of validity
- [ ] Reference data generated by a script in `scripts/`, not committed by hand
- [ ] `julia --project=docs scripts/validate.jl` passes
- [ ] `julia --project=docs scripts/generate_index.jl` run and `index.md` committed
- [ ] Status set to `draft`, or to `ready` with the reference solution in place

**Where it sits in the difficulty order**, and why: <!-- interface motion, then
interface condition, then equations, then domains -->

---

## Correction

**What is wrong**:

**Where the corrected value comes from**:

- [ ] `julia --project=docs scripts/validate.jl` passes
- [ ] If a gated number changed, the provenance is above
